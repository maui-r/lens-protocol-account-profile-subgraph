type Account @entity {
  id: Bytes!
  following: [FollowNFT!]! @derivedFrom(field: "owner")
}

type Profile @entity {
  id: ID!
  followers: [FollowNFT!]! @derivedFrom(field: "profile")
}

type FollowNFT @entity {
  id: ID!
  owner: Account!
  profile: Profile!
  timestamp: BigInt!
}